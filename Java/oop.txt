OOPs Fundamental
Assignment questions

Q1. How to create an object in JAVA ?
Ans :-
We use a new operator to create an object in java.

Q2.  What is the use of new keyword in java?
Ans:- 
New keyword is use to create an object in java.
Let test be a class then a syntax to create a new object 
Will be
Syntax :-
Test t = new Test( );

Q3. What are the different types of variables in java ?
Ans:-
* The different types of variables in java are :
Based on the value represented by a variable all variables are divided into 2 types. They are: 
1.Primitive variables 
2. Reference variables

Primitive variables can be use to represent primitive values.
Ex:- int =10;

Reference variables can be use to refer objects.
Ex : Student s = new Student( );

Based on the behaviour and position of declaration all variables are divided into following types :
 1. Instance variables
 2. Static variables
 3.Local variables.



Q4. What is the difference between instance variable and Local variable?
Ans:-
Instance variables:
If the value of variable is varied from object to object such type of variables are call instance variables.

Local variables:
Sometimes to meet temporary requirements of the programmer, we can declare variables inside a method
or block or constructors such type of variables are called local variables or automatic variables or temporary variables or stack variables.

Q5. In which area memory is allocated for instance variable and local variable?
Ans :-
Instance variables will be stored on the heap as the part of the object.
Local variables can be stored inside the stack.

Q6.What is method overloading?
Ans:-
Two methods are said to be overloaded if and only if both have the same name but different argument types.
Ex: 
abs() for int data type
labs for long data type 
fabs() for float data type




